name: Build Patched HostFXR

on:
  schedule:
  - cron: "0 * * * *"

  workflow_dispatch:

jobs:
  osx-x64:
    name: Build OSX Artifacts
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
        with:
          ref: Patcher
      - run: |
          sudo xcode-select --switch /Applications/Xcode_14.3.1.app/Contents/Developer
          ls -al ./
          mkdir runtime
          cp -r ./HostFXRPatcher/ ./runtime/
          ls -al ./runtime/
