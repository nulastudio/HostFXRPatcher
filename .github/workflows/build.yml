name: Build Patched HostFXR

on:
  schedule:
  - cron: "0 * * * *"

  workflow_dispatch:

jobs:
  osx-x64:
    name: Build OSX Artifacts
    runs-on: macos-13
    steps:
      - run: |
          pwsh --version
